version: '3.9'

services:
  control-plane:
    container_name: control-plane
    image: registry.helix-dev.synadia.io/control-plane:tag-release
    hostname: control-plane
    command: ["-c", "/etc/syn-cp/syn-cp.json", "server", "start"]
    volumes:
      - ./conf/syn-cp:/etc/syn-cp
      - control-plane-data:/app/data
    ports:
      - 8080:8080
      - 8443:8443

volumes:
  control-plane-data: {}
