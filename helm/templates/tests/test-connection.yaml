apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helix.fullname" . }}-test-connection"
  labels:
    {{- include "helix.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helix.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
